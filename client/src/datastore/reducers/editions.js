// TODO create edition-related actions and handle them here
import { LOAD_EDITION, LOAD_CATALOG } from '../actions'

export const initialState = {}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}