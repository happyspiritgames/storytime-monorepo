import React from 'react'
import PropTypes from 'prop-types'

const AddSign = (props) => {
  return (
    <div>Add Sign component</div>
  )
}

AddSign.propTypes = {
  addSign: PropTypes.func
}

// TODO might go this route but have to keep state somewhere else
